language: go

go:
  - "1.12"

env:
  - GO111MODULE=on

before_install:
  - sudo apt-get update
  - sudo apt-get install -y libpcap-dev gcc
  - sudo apt-get install -y snapd
  - export PATH=/snap/bin:$PATH

script:
  - make vet
  - make install-kubectl
  - make all
  - make e2e-tests-environment
  - make test
  - make static-tcpdump

before_deploy:
  - make package
  - git tag "$(cat VERSION)"

deploy:
  provider: releases
  api_key: $GITHUB_OAUTH_TOKEN
  file:
    - ksniff.zip
  skip_cleanup: true
  on:
    tag: true
    branch: master